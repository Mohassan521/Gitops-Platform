apiVersion: v1
kind: Service

metadata:
  name: gitops-platform

spec:
  type: {{ .Values.service.type }}

  selector:
    app: gitops-platform

  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000